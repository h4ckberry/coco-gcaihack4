name: CI

# トリガー: mainブランチへのpush、PR、または手動実行
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  # 何もしないプレースホルダージョブ
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Awaiting Implementation
        run: echo "CI is currently under construction. Passing..."
